name: observable_datasets
description: "Provides wrapper classes around List and Set, implementing ChangeNotifier."
version: 1.0.1
repository: 'https://github.com/Este2013/observable_collections'

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


# The following section is specific to Flutter packages.
flutter: